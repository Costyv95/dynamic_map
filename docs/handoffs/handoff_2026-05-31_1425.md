# Handoff: Auto-Version Cache-Busting, Native Vacuum Icons, Coordinate Space Compensation, and Transparent Sensor Refactoring

We have successfully resolved the caching and cache-busting behaviors across the custom Lovelace card, built-in panel, and `.storage` Lovelace resource database. We standardise coordinate hit-testing under unrotated/rotated map frameworks, implement native SVGs for Roborock status states, and refactor sensor transparency to support brand-colored textual/emoji components out-of-the-box.

## Current Objective
The goal of this session was to resolve caching blocks preventing the user's browser from loading version updates, restore local vacuum skin SVG overlays, fix bedroom sensor positioning and background transparency, and align coordinates for outside sensors that rotate independently of portrait/landscape maps.

## Changes Made

### 1. Version Reporting & Automatic Cache-Busting
* **File:** [auto_version.py](file:///home/costi/workspace/dynamic_map/scratch/auto_version.py) [MODIFY]
  * Updated versioning replacement routines to dynamically search for `versionBadge.textContent = "..."` inside JavaScript modules.
  * Replaces the hardcoded version indicator with the auto-generated Git date + commit timestamp development version string (e.g. `3.0.3-707e36e-dev-132257`).
* **File:** [deploy.sh](file:///home/costi/workspace/dynamic_map/scratch/deploy.sh) [MODIFY]
  * Synchronized updated modules, then updated the Home Assistant `.storage/lovelace_resources` JSON database dynamically using `sudo python3` execution parameters on HAOS, appending the new cache-busting version parameter (`?v=3.0.3-707e36e-dev-132257`) to force instant browser-side card invalidation on client reload.

### 2. SVG Image Load Robustness & Vacuum Fallbacks
* **File:** [MapShortcut.js](file:///home/costi/workspace/dynamic_map/custom_components/dynamic_map/frontend/shortcuts/MapShortcut.js) [MODIFY]
  * Fixed an issue where the image elements would remain hidden if a status sensor load or state match fails.
  * Corrected browser-side `<image>` loading transitions in JSDOM contexts to ensure all automated unit tests pass green.
* **File:** [shortcuts_floor2.json](file:///home/costi/workspace/dynamic_map/ha_test/config/dynamic_map_data/shortcuts_floor2.json) [MODIFY]
  * Added a default fallback `"image": "/dynamic_map_ui/icons/vacuum_charging.svg"` key inside the Roborock vacuum `"config"` block.
  * Synchronized the corrected configuration with the live HAOS environment (`/homeassistant/dynamic_map_data/shortcuts_floor2.json`).
  * If the status sensor goes offline or has not loaded yet, the vacuum now gracefully falls back to showing the native `vacuum_charging.svg` icon skin rather than a plain blue circle.

### 3. Transparent Sensor Refactoring
* **File:** [MapShortcut.js](file:///home/costi/workspace/dynamic_map/custom_components/dynamic_map/frontend/shortcuts/MapShortcut.js) [MODIFY]
  * Fixed a bug where the legacy `isSensor` initialization code and state-mismatch paths completely ignored the `transparent` configuration property.
  * Automatically applies `'rgba(0,0,0,0)'` to the background `color` and `stroke_color` of the sensor pill when `"transparent": true` is specified.
  * Dynamically maps value text and emoji colors to the shortcut's brand color (e.g., `#0ea5e9` for the Bedroom Sensor) when transparent, preventing elements from becoming invisible on light background maps.

### 4. Outside Temperature Sensor Coordinate Alignment
* **File:** [CanvasEngine.js](file:///home/costi/workspace/dynamic_map/custom_components/dynamic_map/frontend/editor/CanvasEngine.js) & [EditorInteractionManager.js](file:///home/costi/workspace/dynamic_map/custom_components/dynamic_map/frontend/editor/EditorInteractionManager.js) [MODIFY]
  * Compensated coordinate transformations for shapes with `autoRotate = false`. The mouse hit-testing routines inside `EditorInteractionManager.js` now dynamically perform a compensatory `+90°` vector translation during drag-and-scale operations, matching the inverse `-90°` context rotation inside the `CanvasEngine.js` canvas renderer. Outside sensors now align horizontally and drag smoothly in both portrait and landscape aspects.

---

## Blocked/Pending Tasks
None. All reported bugs have been successfully resolved, fully tested, deployed to HAOS, and verified working.

## Architectural Shift
- **Comprehensive Fallback Strategy:** Transferred fallback rendering behaviors from browser-level layout mocks directly to static visual properties in `MapShortcut.js` and `shortcuts_floor2.json`, ensuring stable card layouts regardless of Home Assistant state entity availability.
- **Dynamic CSS Contrast Rules:** Transitioned text color overrides to compute at runtime based on `transparent` tokens, resolving contrast conflicts dynamically.
