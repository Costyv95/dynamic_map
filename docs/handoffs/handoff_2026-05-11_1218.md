# Session Handoff: 2026-05-11 12:18

## Current Objective
Stabilize the Dynamic Map UI mapping logic, ensure persistent storage functions correctly, and refine the Lovelace custom card tracking logic to gracefully handle advanced Roborock vacuum states.

## Changes Made
- **`custom_components/dynamic_map/frontend/editor.html`**:
  - Removed the confusing and redundant `saveShortcutBtn` ("Save Config") since the editor inputs are naturally reactive.
  - Renamed the primary export button to "Save to Home Assistant" to clarify persistent storage triggers.
  - Added auto-cleanup logic to purge legacy `room_*` keys from `sc.room_mapping` caused by the previous logic inversion.
- **`custom_components/dynamic_map/frontend/custom-svg-map.js`**:
  - **Cache Busting**: Appended `?t=Date.now()` to the fetch requests for `rooms_floorX.json` to prevent Lovelace from caching stale dashboard data.
  - **Inverted Room Mapping**: Swapped the destructuring variables in the mapping loop to correctly parse `{ roborock_id: svg_id }`.
  - **Binary Sensor Tracking**: Replaced fragile string-matching on the 43 enum states of the vacuum with a boolean check against `binary_sensor.[vac]_charging`.
  - **Advanced Teleport & Wander Animation**: Disabled straight-line interpolation between rooms (which caused the vacuum to "fly through walls"). It now teleports instantly to the center of the new room, and actively wanders within the strict boundaries of the active SVG polygon while cleaning.

## Blocked/Pending Tasks
- **UI Card Editor**: A custom visual editor for the Lovelace card is still needed to avoid manual YAML editing.
- **Frontend File Cleanup**: The backend Python scripts for OpenCV/DXF processing still reside in the `frontend/` directory and need to be isolated.

## Architectural Shift
- Tracking Logic has fundamentally shifted from explicitly checking for a `cleaning` string, to an exclusionary model. The Lovelace card now tracks the room sensor during *any* state, **unless** the hardware explicitly reports that it is attached to the dock via the charging binary sensor.
